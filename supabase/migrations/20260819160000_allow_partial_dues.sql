-- 같은 사람·같은 달에 회비 행이 하나뿐이어야 한다는 제약을 푼다.
--
-- 중복 입력을 막으려고 걸었는데, 앱은 정반대로 동작한다.
-- duesForPeriod() 는 그 달의 회비 행을 전부 더해서 미납액을 내고,
-- 회비 화면의 "입금 처리" 버튼은 남은 금액만큼을 새 행으로 넣는다.
-- 그래서 반만 내고 나중에 마저 내는 흔한 경우가 이 제약에 걸려 저장이 실패했다.
--
-- 중복은 DB가 아니라 검토 화면이 막는다. AI가 읽은 항목은 사람이 체크해야 저장되고,
-- 장부 화면에서 행 단위로 지울 수 있다.

drop index if exists public.ledger_due_once_idx;
